# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
ENV/
pip-log.txt
pip-delete-this-directory.txt
.tox/
.coverage
.coverage.*
htmlcov/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Node.js
node_modules/
npm-debug.log
yarn-error.log
yarn-debug.log
package-lock.json # Add this if you don't want to commit it, though often it is committed
# frontend/dist/ # Example if you have a frontend build output

# IDEs and Editors
.idea/
.vscode/
*.swp
*~
*.sublime-project
*.sublime-workspace

# OS-specific
.DS_Store
Thumbs.db

# n8n - specific (if any, for local data not in workflows)
# Add any n8n specific local data paths here if they arise and shouldn't be in git
# For example, if n8n created local log files outside Docker volumes:
# *.log

# Secrets - ensure no secrets are ever committed
# Double-check any local config files that might contain secrets
# .env # A common pattern for environment variables

# Docker
.dockerignore
docker-compose.yml # If it contains secrets or local overrides; often committed otherwise.
# For this project, dev_setup.md implies docker-compose.yml is not used for core n8n itself.

# OmniPost specific (examples)
# frontend/build/ # if you have a build directory for frontend
# reports/ # if you generate local reports

# Next.js build outputs for frontend
/frontend/.next/
/frontend/out/
/frontend/next-env.d.ts 